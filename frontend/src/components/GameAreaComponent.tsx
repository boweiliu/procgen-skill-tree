import "./GameAreaComponent.css"

import classnames from "classnames";
import React, { useCallback, useMemo, useState } from "react";


export function GameAreaComponent(props: {hidden: boolean}) {
  return (
    <> </>
  )
}